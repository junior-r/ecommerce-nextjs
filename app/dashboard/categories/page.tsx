import DashboardHeader from "@/components/pages/dashboard/header";

type Props = {};

function Page({}: Props) {
    return (
        <DashboardHeader title={"Categories"} description={"categories page"}/>
    );
}

export default Page;
